package io.github.sovathna

import io.github.sovathna.model.Config

object Const {
    const val PAGE_SIZE = 200
    const val DB_DOWNLOAD_URL = "https://github.com/sovathna/database/raw/main/data.zip"

    var config = Config()
}